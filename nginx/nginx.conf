events {}

# configuration for http server
http {
    server {
        listen 80;
        location / {  
            proxy_pass http://pro-app:5000;
            proxy_redirect off;
        }
    }
}

# configuration for https server
# https {
#     server {
#         listen 443 ssl;
#         ssl_certificate /etc/ssl/certs/nginx.crt;
#         ssl_certificate_key /etc/ssl/private/nginx.key;
#         location / {  
#             proxy_pass http://pro-app:5000;
#             proxy_redirect off;
#         }
#     }
# }
